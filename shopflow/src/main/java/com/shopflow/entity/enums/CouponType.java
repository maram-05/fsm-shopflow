package com.shopflow.entity.enums;

/**
 * Type de réduction d'un coupon promo.
 */
public enum CouponType {
    PERCENT,  // Réduction en pourcentage (ex: -20%)
    FIXED     // Réduction en valeur fixe   (ex: -10 TND)
}